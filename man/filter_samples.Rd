% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{filter_samples}
\alias{filter_samples}
\title{Filter a \code{symbayes} object by prevalence and sequencing depth}
\usage{
filter_samples(sp, min_reads = 100, min_prev = 2)
}
\arguments{
\item{sp}{A \code{symbayes} object from \link{import}.}

\item{min_reads}{Minimum total reads per sample (default 100).}

\item{min_prev}{Minimum number of samples a sequence must appear in
(default 2).}
}
\value{
The filtered \code{symbayes} object (\code{filtered = TRUE}).
}
\description{
Soft-filters the count matrix: removes low-prevalence sequences and
low-depth samples, and re-orders sequences by abundance. Unlike SymPortal's
hard minimum-sequence cutoff, \code{min_reads} is a floor applied once, not a
rule that reshapes profiles.
}
\examples{
\dontrun{
sp <- filter_samples(sp, min_reads = 100, min_prev = 2)
}
}
