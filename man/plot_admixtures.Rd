% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-generalist.R
\name{plot_admixtures}
\alias{plot_admixtures}
\title{Admixture plot: mixed samples, sequence mix over boxed component structure}
\usage{
plot_admixtures(
  sp,
  model = "hdp",
  min_frac = 0.05,
  min_groups = 2,
  seq_min_pct = 1,
  sample_label = "sample_name",
  box_colour = "grey15",
  label_profile = TRUE,
  combine = TRUE
)
}
\arguments{
\item{sp}{A \code{symbayes} object.}

\item{model}{One of \code{"dmm"}, \code{"lda"}, \code{"hdp"}.}

\item{min_frac}{Minimum group proportion to count as present (default 0.05).}

\item{min_groups}{Minimum number of present groups for a sample to be shown
(default 2; i.e. only mixed samples).}

\item{seq_min_pct}{Minimum percent in any admixed sample to show a sequence
individually in the top panel (default 1).}

\item{sample_label}{Metadata column for x labels (default \code{"sample_name"}).}

\item{box_colour}{Outline colour for each within-sample component segment
(default \code{"grey15"}).}

\item{label_profile}{If \code{TRUE} (default) and SymPortal profiles are present,
annotate the bottom panel with each sample's dominant SymPortal profile.}

\item{combine}{If \code{TRUE} (default) return a \pkg{patchwork} of both panels;
if \code{FALSE} return a list with \code{seq} and \code{admix} ggplots.}
}
\value{
A \pkg{patchwork} object (or a list if \code{combine = FALSE}).
}
\description{
Filters to samples that are genuinely \emph{mixed} under a fitted model (at least
\code{min_groups} groups above \code{min_frac}) and returns a two-panel figure sharing
one x-axis:
}
\details{
\describe{
\item{Top}{ITS2 \strong{sequence} composition of the admixed samples (a single
stacked barplot, no facet) -- the raw mixture SymPortal sees.}
\item{Bottom}{\strong{Component} proportions (\code{theta}) for the same samples,
with every contributing segment outlined so the within-sample admixture
is demarcated.}
}

Together they show what SymPortal would collapse to a single compound profile
name (top) versus the fractional community structure the mixture model
recovers (bottom). DMM is near-degenerate here (hard assignment), so this is
most informative for \code{"lda"} and \code{"hdp"}.
}
\examples{
\dontrun{ plot_admixtures(sp, model = "lda") }
}
