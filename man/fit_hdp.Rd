% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-hdp.R
\name{fit_hdp}
\alias{fit_hdp}
\title{Fit a Hierarchical Dirichlet Process}
\usage{
fit_hdp(
  sp,
  group_var = NULL,
  n_chains = 4,
  burnin = 5000,
  n = 100,
  space = 50,
  initcc = 10,
  alphaa = 1,
  alphab = 1,
  cos_merge = 0.9,
  min_sample = 1,
  seed = 42
)
}
\arguments{
\item{sp}{A filtered \code{symbayes} object.}

\item{group_var}{Optional metadata column defining a middle DP layer, or
\code{NULL} (default) for a flat, covariate-free structure.}

\item{n_chains}{Independent Gibbs chains (default 4).}

\item{burnin}{Burn-in iterations per chain (default 5000).}

\item{n}{Posterior samples collected per chain (default 100).}

\item{space}{Iterations between collected samples (default 50).}

\item{initcc}{Initial cluster count (default 10; a starting point only).}

\item{alphaa, alphab}{Gamma hyperprior (shape, rate) on DP concentration
(default 1, 1).}

\item{cos_merge}{Cosine similarity above which raw clusters are merged during
extraction (default 0.90; the \pkg{hdp} default). Higher retains more
components. Do not set very low or everything collapses.}

\item{min_sample}{Minimum posterior samples a component must appear in to be
retained (default 1).}

\item{seed}{Base random seed (chains use \code{seed + chain}).}
}
\value{
The \code{symbayes} object with \code{sp$hdp} populated.
}
\description{
The threshold-free, mixture-aware core of \code{symbayes}. Unlike DMM and LDA,
the number of community types is \strong{inferred from the data} rather than
specified: the DP concentration parameters (with Gamma hyperpriors) govern
component number, and samples may be admixed. Uses the \pkg{hdp} package
(Roberts). See \url{https://github.com/nicolaroberts/hdp}.
}
\details{
With \code{group_var = NULL} the structure is flat (one parent DP, one child DP
per sample) and assignment is covariate-free. Supplying a metadata column
(e.g. \code{"host_species"}) inserts a middle DP layer so samples within a group
share strength preferentially. \strong{Caution:} grouping by a covariate you
later want to \emph{test} (e.g. host effect on symbiont community) introduces
circularity; use flat structure for host-blind claims, grouped only as an
explicitly-labelled predictive enhancement.

Writes a membership object to \code{sp$hdp}. \code{theta} excludes the residual
(unassigned) mass, which is kept in \code{meta$residual}. \code{meta} also holds
\code{entropy}, \code{max_prop}, \code{n_comp} per sample, \code{raw_ncl} (sampler cluster
count), and the \code{multi} chain object.
}
\examples{
\dontrun{
sp <- fit_hdp(sp)                       # flat, threshold-free
hdp_plot_numcomp(sp)
hdp_vs_symportal(sp)
}
}
