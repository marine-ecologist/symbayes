% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-generalist.R
\name{plot_symportal_comparison}
\alias{plot_symportal_comparison}
\title{Four-row SymPortal-vs-model comparison over all samples}
\usage{
plot_symportal_comparison(
  sp,
  model = "hdp",
  sample_label = "sample_name",
  box_colour = "grey15",
  show_x = FALSE
)
}
\arguments{
\item{sp}{A \code{symbayes} object with the chosen model fitted and SymPortal
profiles present.}

\item{model}{One of \code{"dmm"}, \code{"lda"}, \code{"hdp"}.}

\item{sample_label}{Metadata column for x labels (default \code{"sample_name"}).}

\item{box_colour}{Outline colour for admixture segments (default \code{"grey15"}).}

\item{show_x}{Draw sample labels on the bottom row (default \code{FALSE}; with
many samples they are unreadable).}
}
\value{
A \pkg{patchwork} object (four rows).
}
\description{
Stacks four aligned panels sharing one sample order (all samples, ordered by
the chosen model's dominant group then confidence):
}
\details{
\enumerate{
\item \strong{Model assignment} -- a single colour band per sample showing the
model's dominant group.
\item \strong{Model admixture} -- boxed component proportions (\code{theta}); each
outlined segment is one component's share within the sample.
\item \strong{SymPortal assignment} -- a single colour band per sample showing
its dominant SymPortal profile.
\item \strong{SymPortal mix} -- stacked SymPortal multi-profile proportions;
samples with more than one profile show the admixture SymPortal records.
}

Rows 1-2 are the model's view (hard then soft); rows 3-4 are SymPortal's
(hard then soft). Reading a column top-to-bottom shows, for one sample, how
the model and SymPortal each assign and each mix it.
}
\examples{
\dontrun{ plot_symportal_comparison(sp, model = "hdp") }
}
