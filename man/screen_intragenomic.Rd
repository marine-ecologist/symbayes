% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-mapping.R
\name{screen_intragenomic}
\alias{screen_intragenomic}
\title{Screen for intragenomic over-splitting and suggest a biological grouping}
\usage{
screen_intragenomic(sp, model = c("lda", "hdp"), deviant_z = 3, ...)
}
\arguments{
\item{sp}{A \code{symbayes} object with the model fitted.}

\item{model}{One of \code{"lda"} or \code{"hdp"}.}

\item{deviant_z}{Robust z-score above which a sample is flagged as deviating
from its group's fixed ratio (default 3).}

\item{...}{Passed to \link{compare_topics} (thresholds).}
}
\value{
Invisibly, a list: \code{pairs} (the compare_topics table), \code{groups}
(data frame mapping each component to a suggested group id), \code{n_units}
(suggested biological unit count), and \code{deviants} (samples breaking their
group's fixed ratio).
}
\description{
Second-stage screen built on \link{compare_topics}. Identifies fixed-ratio
("intragenomic") and redundant pairs, forms their transitive closure into
groups (if 2~9 and 9~5 are both fixed-ratio, then {2,5,9} is one group), and
reports how many biological units the topics reduce to -- as a \strong{suggestion
only}. It does NOT modify \code{theta}: the intragenomic call is provisional
(psbA is the arbiter), and merging on a compositional threshold would
reintroduce exactly the threshold-dependent clustering the package avoids.
}
\details{
For each suggested group it also flags \strong{deviant samples} -- those whose
within-group ratio departs from the group's fixed ratio by more than
\code{deviant_z} robust SDs. These are the samples that break the intragenomic
pattern: either genuine mixing exceptions or low-depth noise, surfaced for
inspection rather than absorbed into the group verdict.

To actually merge a suggested grouping into a coarser membership, call
\link{merge_topics} explicitly with the grouping you choose.
}
\examples{
\dontrun{
scr <- screen_intragenomic(sp, model = "lda")
scr$n_units
scr$groups
scr$deviants
}
}
