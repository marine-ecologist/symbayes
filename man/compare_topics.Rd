% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-mapping.R
\name{compare_topics}
\alias{compare_topics}
\title{Combined pairwise topic comparison: redundancy, mixing, and ratio stability}
\usage{
compare_topics(
  sp,
  model = c("lda", "hdp"),
  redundant_thresh = 0.85,
  rho_intra = 0.03,
  rho_mixing = 0.05,
  min_frac = 0.05,
  min_samples = 5,
  min_pair_reads = 100
)
}
\arguments{
\item{sp}{A \code{symbayes} object with the model fitted.}

\item{model}{One of \code{"lda"} or \code{"hdp"}.}

\item{redundant_thresh}{Cosine at/above which a pair is \code{"redundant"}
(default 0.85).}

\item{rho_intra}{Rho at/below which a co-occurring pair is \code{"intragenomic"}
(default 0.03).}

\item{rho_mixing}{Rho at/above which a co-occurring pair is \code{"mixing"}
(default 0.05).}

\item{min_frac, min_samples, min_pair_reads}{Passed to the overdispersion test.}
}
\value{
Invisibly, a data frame of pairs with all diagnostics and \code{verdict},
ordered by verdict priority then strength.
}
\description{
One row per co-occurring component pair, synthesising the three diagnostics
needed to decide what a pair \emph{is}:
}
\details{
\describe{
\item{\code{cosine}}{Exemplar (beta) cosine similarity. High (> \code{redundant_thresh})
= redundant topics (the same signature split in two).}
\item{\code{n_cooccur}, \code{mean_ratio}, \code{ratio_sd}}{How often the pair co-occurs
and how stable their relative proportion is across those samples.}
\item{\code{rho}}{Beta-binomial overdispersion from \link{test_overdispersion} on real
reads. Low (~0) = fixed ratio (intragenomic); high = variable (mixing).}
\item{\code{verdict}}{A synthesised call (see below).}
}

The verdict combines all three:
\itemize{
\item \code{"redundant"} -- cosine >= \code{redundant_thresh} (merge candidates).
\item \code{"intragenomic"} -- co-occur often at a fixed ratio (rho <=
\code{rho_intra}): likely one symbiont's intragenomic variants split across
topics.
\item \code{"mixing"} -- co-occur with a variable ratio (rho >= \code{rho_mixing}):
genuine community admixture.
\item \code{"ambiguous"} -- co-occur but rho between the thresholds.
\item \code{"distinct"} -- rarely co-occur (< \code{min_samples}): separable types.
}

Compositional evidence alone cannot prove intragenomic vs a fixed-proportion
obligate association; a single-copy marker (psbA) is the definitive arbiter.
}
\examples{
\dontrun{
ct <- compare_topics(sp, model = "lda")
subset(ct, verdict == "intragenomic")
}
}
