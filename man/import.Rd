% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{import}
\alias{import}
\title{Import SymPortal output into a \code{symbayes} object}
\usage{
import(
  seqs_abund,
  profs_abund = NULL,
  profs_meta = NULL,
  seqs_meta = NULL,
  sample_sheet = NULL,
  colour_dict = NULL,
  sheet_skip = 1
)
}
\arguments{
\item{seqs_abund}{Path to \verb{*.seqs.absolute.abund_only.txt} (samples x
post-MED sequence variants). Required.}

\item{profs_abund}{Path to \verb{*.profiles.absolute.abund_only.txt}
(samples x ITS2 type profiles). Optional; enables profile comparison.}

\item{profs_meta}{Path to \verb{*.profiles.meta_only.txt} (profile UID, name,
clade). Optional.}

\item{seqs_meta}{Path to \verb{*.seqs.absolute.meta_only.txt} (per-sample QC).
Optional.}

\item{sample_sheet}{Path to a SymPortal datasheet \code{.xlsx}. Optional; needs
the \pkg{readxl} package.}

\item{colour_dict}{Path to \code{color_dict_post_med.json}. Optional; used for
SymPortal-consistent sequence colours in bar plots.}

\item{sheet_skip}{Header rows to skip in the datasheet (default 1).}
}
\value{
A \code{symbayes} object: a list with \code{count_mat}, \code{prof_mat},
\code{prof_meta}, \code{metadata}, \code{col_dict}, \code{seq_clades}, and \code{filtered = FALSE}.
}
\description{
Reads a SymPortal post-MED sequence count table (the only required input)
and, optionally, ITS2 type profiles, sample metadata, and the SymPortal
colour dictionary. Returns a \code{symbayes} object that flows through the rest
of the pipeline.
}
\examples{
\dontrun{
sp <- import(
  seqs_abund  = "run.seqs.absolute.abund_only.txt",
  profs_abund = "run.profiles.absolute.abund_only.txt",
  profs_meta  = "run.profiles.meta_only.txt",
  colour_dict = "color_dict_post_med.json"
)
sp
}
}
