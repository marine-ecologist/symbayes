% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/name.R
\name{match_profiles}
\alias{match_profiles}
\title{Match model topics/components to SymPortal profiles by composition}
\usage{
match_profiles(sp, model = c("dmm", "lda", "hdp"), top_n = 3)
}
\arguments{
\item{sp}{A \code{symbayes} object with the model fitted and SymPortal profiles
present.}

\item{model}{One of \code{"dmm"}, \code{"lda"}, \code{"hdp"}.}

\item{top_n}{Number of best-matching profiles to report per topic (default 3).}
}
\value{
A data frame: one row per topic-profile match, with \code{group},
\code{topic_name}, \code{sp_profile}, \code{cosine}, and match rank.
}
\description{
String-matching topic names to SymPortal profile names fails: the same
community can be written \code{C22-C3-C22ad} (topic) and \code{C22-C22ad-C3-C22ah}
(SymPortal) -- overlapping sequences, different order and set. This function
instead matches by \emph{composition}: it represents each SymPortal profile as a
sequence vector (from the profile's defining sequences, via \code{prof_meta} if
available, else from the mean sequence composition of samples dominant for
that profile) and computes cosine similarity to each topic's exemplar
(\code{beta}). Each topic is matched to its most similar profile.
}
\examples{
\dontrun{ match_profiles(sp, model = "lda") }
}
