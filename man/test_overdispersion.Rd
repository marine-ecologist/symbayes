% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-mapping.R
\name{test_overdispersion}
\alias{test_overdispersion}
\title{Depth-aware overdispersion test (rebuilt on real read counts)}
\usage{
test_overdispersion(
  sp,
  model = c("lda", "hdp"),
  min_frac = 0.05,
  min_samples = 5,
  min_pair_reads = 100,
  rho_mixing = 0.05,
  top_n = 15,
  owner_frac = 0.5
)
}
\arguments{
\item{sp}{A \code{symbayes} object with the chosen model fitted.}

\item{model}{One of \code{"lda"} or \code{"hdp"}.}

\item{min_frac}{Component presence threshold to count a sample as co-occurring
(default 0.05).}

\item{min_samples}{Minimum co-occurring samples to test a pair (default 5).}

\item{min_pair_reads}{Minimum \code{n_i} (pair reads) to include a sample
(default 100).}

\item{rho_mixing}{Beta-binomial rho above which a pair is called \code{"mixing"}
(default 0.05). Between a small floor and this, \code{"ambiguous"}.}

\item{top_n, owner_frac}{Passed to the internal defining-sequence assignment.}
}
\value{
Invisibly, a data frame: \code{comp_a}, \code{comp_b}, \code{n_samples},
\code{n_seqs_a}, \code{n_seqs_b}, \code{mean_ratio}, \code{rho}, \code{phi}, \code{p_value}, \code{call}.
}
\description{
Rebuilt to use \strong{actual sequence read counts} rather than \code{theta * depth}.
Each component is represented by its \emph{defining sequences} (those it clearly
owns in its exemplar). For a pair (A, B), \code{k_i} = sample i's summed reads of
A's defining sequences and \code{n_i} = reads of A's + B's defining sequences --
genuine integer read counts, so the binomial sampling model is legitimate.
}
\details{
Overdispersion is estimated as the beta-binomial intra-class correlation
\eqn{\rho} (method of moments), which bounds in \eqn{[0, 1)} and properly
separates true cross-sample ratio variance from within-sample sampling
variance. \eqn{\rho \approx 0} = fixed ratio (consistent with intragenomic
variation); \eqn{\rho} clearly above 0 = variable ratio (consistent with
community mixing). A dispersion index \code{phi} (Pearson X^2 / df) is reported
alongside as a cross-check -- now well-behaved because \code{n_i} are real counts.
}
\examples{
\dontrun{
od <- test_overdispersion(sp, model = "lda")
subset(od, call == "mixing")
}
}
