% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-mapping.R
\name{plot_overdispersion}
\alias{plot_overdispersion}
\title{Plot the overdispersion result for one component pair (real read counts)}
\usage{
plot_overdispersion(
  sp,
  model = c("lda", "hdp"),
  comp_a,
  comp_b,
  min_frac = 0.05,
  min_pair_reads = 100,
  top_n = 15,
  owner_frac = 0.5
)
}
\arguments{
\item{sp}{A \code{symbayes} object with the model fitted.}

\item{model}{One of \code{"lda"} or \code{"hdp"}.}

\item{comp_a, comp_b}{Component labels (e.g. \code{"Topic_2"}, \code{"Topic_9"}).}

\item{min_frac}{Presence threshold (default 0.05).}

\item{min_pair_reads}{Minimum pair reads to include a sample (default 100).}

\item{top_n, owner_frac}{Defining-sequence assignment controls.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Each co-occurring sample's observed A-share from real defining-sequence
reads, vs its pair read depth, with the binomial fixed-ratio null envelope
(+/-2 SE around the pooled ratio). Points tracking the band at all depths =
fixed ratio (intragenomic); systematic spread beyond it, especially at high
depth where the band is tight, = genuine mixing.
}
\examples{
\dontrun{ plot_overdispersion(sp, "lda", "Topic_2", "Topic_9") }
}
