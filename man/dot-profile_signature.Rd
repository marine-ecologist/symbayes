% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{.profile_signature}
\alias{.profile_signature}
\title{Stable cross-run signature for a SymPortal profile}
\usage{
.profile_signature(accession_str, n_lead = 3)
}
\arguments{
\item{accession_str}{The \verb{Sequence accession / SymPortal UID} string, e.g.
\code{"62825/742546-32-1147953"}.}

\item{n_lead}{Number of leading accessions to use (default 3).}
}
\value{
A signature string (leading accessions, sorted, joined by \verb{_}).
}
\description{
SymPortal profile UIDs are run-specific, but the defining-sequence
\emph{accessions} are database-level identifiers stable across runs. The leading
accessions (the majority / co-dominant sequences) are the intrinsic identity;
the trailing minor DIVs are run-context-dependent (a minor sequence is
appended only if it co-occurred often enough in that run's samples). This
builds a signature from the first \code{n_lead} accessions, so the same biological
profile matches across runs even when its minor-DIV tail differs.
}
\keyword{internal}
