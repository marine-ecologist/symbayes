% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-dmm.R
\name{fit_dmm}
\alias{fit_dmm}
\title{Fit a Dirichlet-Multinomial Mixture model}
\usage{
fit_dmm(sp, max_k = 10, seed = 42)
}
\arguments{
\item{sp}{A filtered \code{symbayes} object.}

\item{max_k}{Maximum number of components to evaluate (default 10).}

\item{seed}{Random seed (default 42).}
}
\value{
The \code{symbayes} object with \code{sp$dmm} populated.
}
\description{
Fits DMMs for \code{k = 1:max_k}, selects the number of components by the Laplace
approximation to the model evidence (with BIC reported alongside), extracts
posterior membership, and, if SymPortal profiles are present, maps each
sample's dominant profile and clade.
}
\details{
DMM is a \emph{hard-assignment} model: each sample is assigned to one component.
Its posterior certainty is often near 1.0 even for mixed samples, because it
models between-sample variation, not within-sample mixing. Use \link{fit_lda} or
\link{fit_hdp} to represent admixture. See \link{dmm_loo} and \link{dmm_simulate} to probe
assignment stability.

Writes a membership object to \code{sp$dmm} with the standard schema
(\code{theta}, \code{beta}, \code{dominant}, \code{k}, \code{meta}). The \code{meta} list contains
\code{certainty} (per-sample max posterior), \code{model_fit} (the K-selection table),
and \code{best_fit} (the fitted \code{DMN} object, used by \link{dmm_predict} etc.).
}
\examples{
\dontrun{
sp <- fit_dmm(sp, max_k = 10)
sp$dmm$k
dmm_plot_selection(sp)
}
}
