% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-dmm.R
\name{dmm_simulate}
\alias{dmm_simulate}
\title{Simulate samples and test DMM assignment (blend / perturb / from_alpha)}
\usage{
dmm_simulate(
  sp,
  mode = c("from_alpha", "blend", "perturb"),
  n_sim = 100,
  lib_size = 5000,
  perturb_frac = 0.1,
  blend_comps = c(1, 2),
  blend_range = seq(0, 1, 0.1),
  seed = 42
)
}
\arguments{
\item{sp}{A \code{symbayes} object with DMM fitted.}

\item{mode}{One of \code{"from_alpha"}, \code{"blend"}, \code{"perturb"}.}

\item{n_sim}{Number of simulated samples (default 100).}

\item{lib_size}{Reads per simulated sample (default 5000).}

\item{perturb_frac}{Fraction of reads to reassign for \code{"perturb"}
(default 0.1).}

\item{blend_comps}{Two component indices to mix for \code{"blend"}
(default \code{c(1, 2)}).}

\item{blend_range}{Mixing proportions for \code{"blend"}
(default \code{seq(0, 1, 0.1)}).}

\item{seed}{Random seed (default 42).}
}
\value{
A data frame of simulated samples with predicted assignments.
}
\description{
Three modes for probing DMM behaviour:
\code{from_alpha} draws from each component's Dirichlet then multinomial;
\code{blend} mixes two components across a gradient to trace decision boundaries;
\code{perturb} reassigns a fraction of reads in real samples to test robustness.
}
\examples{
\dontrun{
dmm_simulate(sp, mode = "blend", blend_comps = c(1, 4))
dmm_simulate(sp, mode = "perturb", perturb_frac = 0.1)
}
}
