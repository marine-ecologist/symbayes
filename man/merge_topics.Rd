% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate-mapping.R
\name{merge_topics}
\alias{merge_topics}
\title{Merge topics into a coarser membership (explicit, user-directed)}
\usage{
merge_topics(sp, model = c("lda", "hdp"), groups)
}
\arguments{
\item{sp}{A \code{symbayes} object with the model fitted.}

\item{model}{One of \code{"lda"} or \code{"hdp"}.}

\item{groups}{Either a data frame with \code{component} and \code{group} columns (as
returned by \code{screen_intragenomic()$groups}), or a named integer vector
mapping component label -> group id.}
}
\value{
A matrix (samples x merged groups) of summed memberships. Column
names list the merged components. Returned, not written into \code{sp}.
}
\description{
Applies a grouping (e.g. from \link{screen_intragenomic}) by summing the theta
columns of components in the same group, producing a coarser membership
matrix. This is deliberately a separate, explicit call -- the package never
merges automatically, because merging on a compositional threshold is a
judgement the user must own (and ideally confirm with psbA).
}
\examples{
\dontrun{
scr <- screen_intragenomic(sp, model = "lda")
merged_theta <- merge_topics(sp, model = "lda", groups = scr$groups)
}
}
