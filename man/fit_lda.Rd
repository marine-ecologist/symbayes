% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-lda.R
\name{fit_lda}
\alias{fit_lda}
\title{Fit a Latent Dirichlet Allocation topic model}
\usage{
fit_lda(
  sp,
  k = NULL,
  k_range = 2:15,
  redundant_thresh = 0.85,
  burnin = 1000,
  iter = 2000,
  thin = 5,
  seed = 42
)
}
\arguments{
\item{sp}{A filtered \code{symbayes} object.}

\item{k}{Fixed number of topics, or \code{NULL} (default) to select adaptively.}

\item{k_range}{Integer vector of K to search when \code{k = NULL} (default 2:15).}

\item{redundant_thresh}{Cosine similarity above which two topics are deemed
redundant (default 0.85).}

\item{burnin, iter, thin}{Gibbs sampler controls (defaults 1000, 2000, 5).}

\item{seed}{Random seed (default 42).}
}
\value{
The \code{symbayes} object with \code{sp$lda} populated.
}
\description{
Fits LDA by Gibbs sampling. Unlike DMM, LDA gives \emph{fractional} membership:
each sample is a mixture of topics, so within-sample admixture is
represented directly.
}
\details{
Topic number selection is threshold-free-oriented: with \code{k = NULL} (default)
the function searches \code{k_range} and picks the \strong{largest k with no redundant
topic pairs} (cosine similarity \verb{< redundant_thresh}). This avoids the
"shadow topics" that perplexity-optimal K tends to produce. Pass an integer
\code{k} to fix it. The search table is stored in \code{sp$lda$meta$selection} and can
be plotted with \link{lda_plot_ksearch}.

Writes a membership object to \code{sp$lda}. \code{meta} contains \code{entropy},
\code{max_prop}, \code{n_topics} (per sample), \code{similarity} (topic cosine matrix),
\code{redundant} (flagged pairs), and \code{selection} (the K-search table).
}
\examples{
\dontrun{
sp <- fit_lda(sp)              # adaptive K
sp <- fit_lda(sp, k = 9)       # fixed K
lda_plot_ksearch(sp)
lda_plot_similarity(sp)
}
}
