% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hdp.R
\name{hdp_vs_symportal}
\alias{hdp_vs_symportal}
\title{Quantify SymPortal over-classification relative to HDP}
\usage{
hdp_vs_symportal(sp, over_split_min_frac = 0.7, mixture_thresh = 0.7)
}
\arguments{
\item{sp}{A \code{symbayes} object with HDP fitted and SymPortal profiles present.}

\item{over_split_min_frac}{Minimum dominant-component share for a profile to
count as over-split (default 0.7).}

\item{mixture_thresh}{Dominant-component share below which a profile is a
mixture (default 0.7).}
}
\value{
Invisibly, a list with \code{n_sp_profiles}, \code{n_hdp_comp},
\code{oversplit_ratio}, \code{mapping}, \code{redundant_splits}, \code{mixture_profiles}
(profile-level), \code{mixture_samples} (one row per sample whose dominant
profile is a mixture, with ranked \code{comp_i}/\code{frac_i} columns showing which
HDP components co-occur in that individual sample), and \code{orphan_profiles}.
}
\description{
The core method metric. Compares SymPortal's profile count to HDP's inferred
component count and, crucially, partitions SymPortal profiles into three
mutually exclusive classes so they do not contaminate one another:
}
\details{
\describe{
\item{Over-splitting}{Several profiles that \emph{confidently} map to one HDP
component (\code{dominant_frac >= over_split_min_frac}). SymPortal split one
real community type into multiple named profiles.}
\item{Mixtures}{Profiles whose samples spread across components
(\code{dominant_frac < mixture_thresh}). SymPortal called an admixture a
unique type; these are not force-assigned.}
\item{Orphans}{Not confidently in any component and not clear mixtures
(usually rare, n = 1). Reported separately and excluded from the
over-splitting count.}
}

The partition matters: a rare profile whose single sample has its largest
component at only a few percent must not be counted as "over-split into"
that component.
}
\examples{
\dontrun{ res <- hdp_vs_symportal(sp) }
}
