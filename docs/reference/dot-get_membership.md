# Retrieve a fitted model's membership object from an `sp` object

Retrieve a fitted model's membership object from an `sp` object

## Usage

``` r
.get_membership(sp, model)
```

## Arguments

- sp:

  A `symbayes` object

- model:

  One of "dmm", "lda", "hdp"

## Value

The membership object
