# LDA K-search over a range, scoring redundancy and pure samples

LDA K-search over a range, scoring redundancy and pure samples

## Usage

``` r
.lda_search(dtm, k_range, redundant_thresh, control)
```
