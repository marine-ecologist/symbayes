# Dirichlet-Multinomial log-likelihood of a count vector under one component

Dirichlet-Multinomial log-likelihood of a count vector under one
component

## Usage

``` r
.dmm_loglik(counts, alpha)
```
